

	<%= link_to 'Edit auditorium', edit_manage_auditorium_path(@auditorium), class: "btn btn-primary float-right"  %>
	<%= link_to 'Add Movie', new_manage_movie_path, class: "btn btn-success float-right"  %>
	<h1><%= @auditorium.name %></h1>
	<small>capacity: <%= @auditorium.capacity %></small>
<hr/>
<h2>Now Showing</h2>
<ul class="list-group">
	<% @auditorium.movies.each do |movie| %>
		<li class="list-group-item"><%= link_to movie, manage_movie_path(movie) %></li>
	<% end %>
</ul>